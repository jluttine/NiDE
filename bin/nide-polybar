#!/bin/sh

# We need to wrap Polybar startup inside a shell script so that we have access
# to tilde (~) or $HOME which are not available in i3 config

exec polybar --reload --config="$HOME/.nide/config/polybar/config" nide